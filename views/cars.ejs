<!DOCTYPE html>
<html lang="en">

<head>
    <%- include('./partials/head'); %>
</head>

<body>
    <div class="container">
        <h1>Cars</h1>
        <% if (cars.length > 1) { %>
        <ul>
            <% cars.forEach(function(car) { %>
            <li>
                <strong><%= car.name %></strong>
                <strong><%= car.url %></strong>
            </li>
            <% }); %>
        </ul>
        <% } %>
    </div>
    <script></script>
</body>

</html>